# LINEA BASE DE LA SUITE (regla R6 — solo puede subir)

Antes de CADA commit: suite completa, comparar contra la ultima cifra de esta
tabla. Bajada = bloqueo (no se committea) + investigacion. Registrar la cifra
nueva aqui y en el informe del bloque.

| Fecha (UTC) | Cifra | Evento |
|---|---|---|
| 2026-07-02 | 603 passed / 7 skipped | [[LINEA_BASE_TESTS|Linea base]] Bloque 0 (canonico) |
| 2026-07-03 | 640 passed / 7 skipped | Cierre Bloques 1-5 |
| 2026-07-03 | 669 passed / 7 skipped | Fase 2 (fixes + 10 [[CUBOS|cubos]]) |
| 2026-07-03 | 680 passed / 7 skipped | Centro + alta + [[MESA_DEL_JEFE|Mesa del Jefe]] (S1) |
| 2026-07-03 | 680 passed / 7 skipped | Verificada tras incidente quota-voz (DT-26.07.03-1) |
| 2026-07-03 | 685 passed / 7 skipped | S2 [[RITUAL_DE_LA_MANANA|ritual de la manana]] (+5 tests) |
| 2026-07-10 | 689 passed / 7 skipped | Tests B1 de la serie D: walk CLI + 3 regresiones ([[AUDITORIA_D00]]) |
| 2026-07-10 | 772 passed / 7 skipped | Construccion serie D: sustrato B2-B7 + cubos (ver [[AUDITORIA_CONSTRUCCION_SERIE_D]]) |
| 2026-07-10 | 796 passed / 7 skipped | Centro de Mando D10 v0.2 construido P0-P5 (panel_mando/, 24 tests; P5 humano pendiente R9+90s) |
| 2026-07-12 | 798 passed / 7 skipped | Cierre P0 Sala: NFC en core/rue + vector dorado (+2 tests, [[INFORME_CIERRE_P0_SALA]]). Reproducida en clon limpio el 2026-07-20: "798 passed, 7 skipped in 7.01s" (registro de la fila: 2026-07-20) |
| 2026-07-20 | 813 passed / 7 skipped | F1 panel honesto: auth de sesion (R-01), dinero real con ledger persistente (R-07), PARAR TODO con evento sellado y persistente (R-08) — 15 tests nuevos ([[ROADMAP_PRODUCCION]] F1); base 798/7 reproducida antes en clon limpio esta misma sesion |
| 2026-07-20 | 829 passed / 7 skipped | F2 los 6 P0 de seguridad: voz CLI neutralizada+candado de baja (R-02), comite anti-inyeccion+quorum (R-03/R-14), Robinson fail-closed (R-04), freno de coste delante con ledger (R-05), webhooks derivan tenant real (R-06), DDL sin default 'laboratorio' (R-18), Verifactu raise real (R-11) — 16 tests nuevos |
| 2026-07-20 | 845 passed / 7 skipped | F3 cableado: email exactamente-una-vez claim CAS + TTL de aprobacion (R-09), candado AI Act art.50 por fecha 2026-08-02 (E4), dinero fiscal Decimal ROUND_HALF_UP + IVA parametrizable (R-12), marca por tenant (R-10), ejecutor con barreras en orden AIACT->coste->panico->envio (R-16) — 16 tests nuevos |
| 2026-07-20 | 857 passed / 7 skipped | F4 entregabilidad E3 (analisis dig SPF/DKIM/DMARC + rampa <=10/dia persistente + dominio-frio=PARAR, 7 tests) y F7 CSRF por sesion + identidad de sesion (R-19, 5 tests, enforcement opt-in). F5/F6/S8: paquete de despliegue + primera-ejecucion armada + checklist/manual (docs; frontera = decisiones de Ivan) |
| 2026-08-05 | **966 passed / 7 skipped / 0 failed**, **SIN datos de cliente en el arbol** | **VIGENTE.** R-TENANT **F5 — desalojo**. Los datos reales salieron del arbol con `mv` (jamas `rm`) a `KAIZEN_DATOS/` (directorio hermano): `empresas/{laboratorio,segundo_tenant,tenants.json}`, `diario/{laboratorio,segundo_tenant,verigest,demo-soft}`, `data/`, `state/`, `bitacora/`, argumentario e ICP de cliente, `CONTEXTO_TOTAL.txt`, la revision de llamada real y 2 ficheros de `_workspace`. **F2-bis:** parametrizadas las otras cuatro raices (`state/`, `data/`, `bitacora/`, `diario/`) — 31 puntos, todas las constantes de modulo convertidas en resolucion por llamada. **Tenant sintetico completado** con `icp.json` y su propio `diario/` (sin el, la simulacion de prospeccion daba cero leads y el circuito no se podia ensayar sin un cliente real). **319 sustituciones en 51 ficheros de test**: la suite ya no nombra a ningun cliente. `.gitignore` blindado (`empresas/*` y `diario/*` salvo lo sintetico). Centinela con la regla R-TENANT **activa** (ya no dormida: la raiz de datos existe). Residuo declarado: los snapshots de despliegue de voz y los textos AI Act siguen nombrando al tenant con el que se desplegaron — decision del operador, §11.2. |
| 2026-08-04 | 966 passed / 7 skipped / 0 failed (con datos)<br>965 / 8 / 0 (sin datos) | R-TENANT F0-F4. R-TENANT F0-F4 (`ESPECIFICACION_R-TENANT_2026-08-04.md`): el producto deja de saber QUE tenant es. F0 indice resincronizado (`git reset`; 17 falsas deleciones, sha256 HEAD==disco en las 17). F2 `core/rutas.py` como punto unico de la raiz de datos (`KAIZEN_DATOS`), sin caida silenciosa al arbol; 9 puntos de construccion de ruta reenrutados (la spec estimaba 6). F3 tenant sintetico `laboratorio` completado a equivalente funcional de un tenant real: `argumentario.json` + los 5 de `brand/` con el esquema real, emitidos por el generador determinista. F4 47 defectos `= "laboratorio"` invertidos a `laboratorio`, DEFAULT del DDL retirado (`cliente_id` sigue NOT NULL), `cliente_id` fuera de los 10 manifests y del esquema de `validar_manifest`, literales de marca fuera de `kaizen.py`/`sustrato/cli.py`/`mesa_jefe.py`/prompts. **Dos fugas nuevas cerradas:** el analisis de tono de VOZ seguia clavado a un tenant (mismo defecto que BG-257/R-10, arreglado solo en el camino de email), y `mesa proponer-demo` seleccionaba leads SIN filtrar por `cliente_id`. Gate anti-literales sin excepciones + regla R-TENANT en el centinela pre-commit, con la lista de vetados derivada de la raiz de datos (escribirla en el arbol seria la propia violacion). **+5 tests.** La diferencia de 1 entre ambas cifras es el gate anti-literales, que se salta cuando la raiz de datos no es accesible. |
| 2026-08-02 | 916 passed / 7 skipped / **1 failed** | Historico. Auditoria completa (51 hallazgos, `docs/AUDITORIA_2026-08-02.md`) + arreglos: F-02 TTL de aprobacion, A-06/A-07/A-08 entorno reproducible (pytest en .venv, `pytest.ini`, `tzdata` — 6 modulos no importaban en Windows limpio), G-02 anti-inyeccion en el comite, G-03 literalidad AI Act por igualdad, G-04 `NO_LLAMAR` solo a COLD con motivo auditado, G-05 CSRF activo por defecto, G-06 sesiones con caducidad + `/logout`, G-07 matriz de acciones inmutable, G-08 cadena de hash bajo concurrencia (BEGIN IMMEDIATE), G-09 at-least-once real + cola de muertos, G-10+B-03+C-12 reserva atomica de presupuesto en los 3 contadores, A-01 repositorio recuperado (66 commits). **+59 tests** (4 ficheros `test_g*`). EL 1 FAILED NO ES REGRESION: es `test_comercial_email_guard`, que cae porque el candado AI Act art.50 entro en vigor HOY por fecha — exactamente lo previsto en la fila del 2026-07-20 (E4). El canal de email queda fail-closed hasta que el operador apruebe un texto de transparencia PARA EMAIL (las 20 variantes existentes son aperturas de voz). Ver F-01 en la auditoria. |
| 2026-08-07 | 983 passed / 7 skipped / 0 failed | Auditoria profunda de produccion (75 agentes de caza + verificacion adversarial independiente por hallazgo): 76 bugs confirmados (19 P0 / 34 P1 / 23 P2), 61 reparados en 11 grupos paralelos por fichero + 12 en `panel_mando/app.py`/`colmena.py` a mano. P0 mas importante: `ColaSustrato.ejecutar()` marcaba EJECUTADA una IRREVERSIBLE-EXTERNA sin que nada saliera jamas al mundo real — ahora usa el estado que devuelve el ejecutor (ENSAYO_SECO honesto sin canal real cableado; ver test_p1_panel_gobierno.py). Otros P0 cerrados: 2 XSS reflejados (`empresa`/`tema` sin escapar en panel y Colmena), modo "sin token" invocable sin credencial (chequeo de Origin/Referer nuevo), `/send` enviaba SMTP real saltandose AI Act/Guardian/sandbox, candado AI Act por substring (una frase que NIEGA ser IA lo satisfacia), `get_cheap_model()` caia a Opus por error, tarifa de Haiku desincronizada 4x entre dos ficheros, 3 comandos de `kaizen.py` rotos por ruta de knowledge hardcodeada (R-TENANT), 4 puntos sin candado entre procesos (JsonKnowledge, LedgerCoste, VoteHistory/HashChain, cola de aprobacion comercial R-09), `KAIZEN.lnk` apuntaba a `Kaizen-REAL.cmd` (gasto real sin avisar), "Pausar campaña" no pausaba nada. R6: 3 tests que codificaban bugs como comportamiento correcto se corrigieron (no se borro cobertura). Bug adicional cazado durante la verificacion (no en la lista de 76): `claude_client.py` cargaba `.env` en tiempo de importacion, filtrando `KAIZEN_TOKEN` real al entorno compartido de tests — aislado en `tests/conftest.py`. Nuevo tipo de evento sellado `plataforma.mandato.actualizado` registrado en `eventos.json` (RUE). |
| 2026-08-07 | 1294 passed / 7 skipped / 0 failed | Historico (superada por la fila siguiente). Fase 4 — directores plenipotenciarios: cada director de Colmena puede invocar herramientas REALES de su propio cubo, y solo del suyo. Nucleo (`panel_mando/herramientas/base.py`): `ToolSpec`/`Argumento`, `invocar()` (LECTURA/REVERSIBLE, aislamiento estricto por cubo — sin fallback entre cubos aunque el LLM nombre otro), `invocar_aprobada()` (IRREVERSIBLE-INTERNA, solo desde el ejecutor de aprobaciones, jamas desde un turno de chat). Protocolo `[HERRAMIENTA]` en `colmena.py`: LECTURA en dos pasadas (el director ve el resultado real antes de responder), REVERSIBLE ejecutada al instante sin pedir permiso (autonomia BAJA), defensa en profundidad en codigo (no solo en el prompt) contra que un cubo CERO ejecute REVERSIBLE. `[PROPUESTA]` extendido con vinculo `herramienta`+`argumentos`: al aprobarse una IRREVERSIBLE-INTERNA con vinculo, se dispara de verdad con los argumentos EXACTOS vistos al proponer (tabla `colmena_propuestas_herramienta`, nunca regenerados por el LLM en el momento de ejecutar) — cierra el hueco de honestidad de la fila anterior para las que si tienen herramienta real. IRREVERSIBLE-EXTERNA sigue SIN wirear en ningun cubo: frontera deliberada R2, el chat no dispara nada al mundo real. 10 registros por cubo construidos en paralelo (105 herramientas reales verificadas contra el codigo real, con criterio de seguridad: saltadas por diseno las que dejarian a un director autoafirmar un gate humano, atestiguar un hecho fisico, mover dinero sin evidencia, o sobrescribir datos sin control — el detalle de cada salto esta en el reporte de cada `tests/test_herramientas_<cubo>.py`). Cubos CERO (legal/qa/rrhh/inteligencia) quedan LECTURA-only en la practica por su propio manifest, aunque legal (que absorbe tambien cumplimiento) tiene algunas IRREVERSIBLE-INTERNA registradas para cuando el operador suba su autonomia. **+311 tests** (10 `test_herramientas_<cubo>.py` + `test_herramientas_base.py` + `test_colmena_herramientas.py`). |
| 2026-08-07 | **1344 passed / 7 skipped / 0 failed** | **VIGENTE.** Excelencia sobre Fase 4 (autoevaluacion critica pedida por el operador: "si fuera para ti, ¿estaria bien?"). Fase 0-2 (locks en `Mechas`/`bit()`/`cola()`, `to_thread` en los 5 puntos donde una herramienta o `quemar_vencidas()` podia bloquear el event loop entero de uvicorn para TODAS las empresas, contabilidad de coste completa incluyendo el gasto interno de `H.invocar`). Fase 3 "servidor fuerza el actor" en 3 sitios mas (replicando el patron ya usado en `legal.py::_fn_cumplir_obligacion`): `marketing.py::lanzar` fuerza `aprobar_campana(por="operador")` en servidor solo si la campaña esta en BORRADOR (no reabre PAUSADA/CANCELADA); `inteligencia.py` gana `definir_umbral_alerta` (IRREVERSIBLE-INTERNA, antes `detectar()` siempre devolvia `None` sin ella); `brand.py::transicionar_directriz` deja de aceptar `por` del LLM. Fase 4: puente aditivo `departments/comercial/puente_canonico.py` conecta el Researcher al modelo canonico con gates S1/S2 (dual-write, nunca rompe el `LeadStore` legado — todo bajo `try/except` amplio). Fase 5: fusion Legal+Cumplimiento completada en los 5 sitios que quedaban a medias (`NOMBRES["legal"]`, `produce_rue` en el manifest, autonomia CERO→BAJA, `cubos/base.py::salud()` cuenta tambien eventos del canal Bitacora, retirado el endpoint `/cmd/cumplimiento/obligacion` — subconjunto mas pobre de lo que ya wireaba `legal.py`). Flujos de datos rotos cerrados: Comercial→CustomerSuccess ahora emite `DEPT_TASK_COMPLETED` de verdad al llegar a CUSTOMER (antes no habia ningun emisor real, solo un listener esperando en vano); `marketing::piezas()` leia el tipo legacy `"contenido"` sin escritor real en vez de `"contenido_mkt"`; `ops.py` gana `declarar_stock` (antes `alerta_stock` no tenia productor). RRHH: `mapa`/`rendimiento`/`propuestas` dejan de reconstruir un `RRHHDepartment` vacio sobre un bus desechable y leen estado real y persistente (`colmena_agentes` + `cubos.base.manifiestos_instalados()`); `rendimiento` ahora dice `datos_disponibles: false` en vez de fingir "sin alertas". QA: pipeline de puntuacion 0-10 activado tras confirmar linea a linea que los 3 especialistas son 100% deterministas (cero llamadas LLM) — las reglas duras legacy siguen intactas para payloads sin `artefacto`. Arreglos de honestidad: `Argumento.validar` ya no aceptaba `bool("false")` como `True`; la ficha del director en Colmena muestra sus capacidades REALES (antes decia "propone; jamas ejecuta", falso desde Fase 4); mensajes de sistema de exito/rechazo/nota ganan distincion visual (antes la misma pildora gris); confirmacion visible cuando una LECTURA se ejecuta con exito; aviso cuando una mencion `@algo` no resuelve a ningun cubo (antes caia en broadcast mudo); techo de coste por tenant respetado en Colmena (antes solo el global compartido). **Bug real cazado durante la verificacion** (fuera de la lista de fases, no relacionado con Colmena): `tests/test_voz_eleven_outbound.py` no aislaba `KAIZEN_VOZ_QUOTA_DIR` — cada ejecucion de la suite completa consumia la quota REAL de llamadas salientes (`KAIZEN_DATOS/state/voz/quota/`, la misma que usaria la app en produccion), acumulada durante dias de sesiones hasta agotar 25/25 hoy mismo; aislado (mismo patron que `test_voz_pre_flight.py`) y el contador de hoy reseteado a 0 (backup del valor previo conservado junto al fichero). **Verificacion adversarial independiente**: 4 workflows de verificacion (uno por grupo de cambios) releyeron el codigo real sin fiarse de los autoinformes — veredicto **SEGURO** en los 4, sin hallazgos criticos (2 notas de calidad de dato, ninguna brecha de seguridad). **R-TENANT centinela** (`--staged`, patron stage→check→restore): mismas 16 violaciones preexistentes en docs historicos, ninguna nueva. **Verificado en navegador real** (servidor de demo con LLM simulado, coste 0€): ficha de un director mostrando sus herramientas reales por clase; mensaje de sistema de exito (verde) visualmente distinto de un rechazo (rojo); flujo completo de Marketing — proponer "lanzar" en el chat → tarjeta PENDIENTE → aprobar (mismo gesto real de mantener pulsado) → `EJECUTADA` → consulta LECTURA posterior confirma la pieza real en `estado: "LANZADO"`, no un error 409 como antes de Fase 3. **+50 tests** sobre la fila anterior. |
