# Dependencias de desarrollo (suite de tests). No necesarias en runtime.
#   .venv/Scripts/python.exe -m pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

pytest>=8.0.0
pytest-asyncio>=0.24.0    # tests async del panel (api/server.py)

# Requeridos por los tests del panel FastAPI (TestClient + WebSocket).
# httpx ya está en requirements.txt; starlette lo arrastra fastapi.
